version: 0.2
phases:

  install:
    commands:
     - cd ts-infra
     - npm ci
  pre_build:
    commands:
     - npm install -g aws-cdk

  build:
    commands:
      - cdk -a deploy backendStack --require-approval=never --verbose